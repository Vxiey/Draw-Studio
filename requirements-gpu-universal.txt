# Optional vendor-neutral GPU benchmark backend for Image Draw Bot Step 22.
# PyOpenCL uses the OpenCL runtime already provided by the installed NVIDIA,
# AMD or Intel graphics driver. Image Draw Bot never installs a system GPU driver.
pyopencl>=2024.3,<2027
