# Optional vendor-neutral GPU benchmark backend for Draw Studio Step 22.
# PyOpenCL uses the OpenCL runtime already provided by the installed NVIDIA,
# AMD or Intel graphics driver. Draw Studio never installs a system GPU driver.
pyopencl>=2024.3,<2027
